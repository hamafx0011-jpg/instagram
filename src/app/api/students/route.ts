import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {studentSchema} from '@/lib/domain';
export async function GET(req:Request){const {searchParams}=new URL(req.url);const q=searchParams.get('q')??'';const students=await prisma.student.findMany({where:q?{OR:[{fullName:{contains:q,mode:'insensitive'}},{studentCode:{contains:q,mode:'insensitive'}},{phone:{contains:q}}]}:{},take:25,orderBy:{createdAt:'desc'}});return NextResponse.json({data:students})}
export async function POST(req:Request){const body=studentSchema.parse(await req.json());const student=await prisma.student.create({data:{...body,studentCode:`ST-${Date.now()}`,email:body.email||null}});await prisma.auditLog.create({data:{action:'CREATE',entity:'Student',entityId:student.id}});return NextResponse.json({data:student},{status:201})}
