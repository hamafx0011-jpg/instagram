import {Shell} from '@/components/shell';const tabs=['Overview','Personal Information','Lessons','Attendance','Payments','Documents','Exams','Progress','Notifications','Activity History'];export default function Page({params}:{params:{locale:string,id:string}}){return <Shell locale={params.locale}><h1 className="text-3xl font-black">Student {params.id}</h1><div className="mt-6 card"><div className="flex flex-wrap gap-2">{tabs.map(tab=><button className="btn border border-slate-200 dark:border-slate-700" key={tab}>{tab}</button>)}</div><p className="mt-6 text-slate-500">Complete profile, lesson consumption, invoices, documents, exams and audit history are represented in Prisma relations.</p></div></Shell>}
