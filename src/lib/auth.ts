import bcrypt from 'bcryptjs';import type {NextAuthOptions} from 'next-auth';import CredentialsProvider from 'next-auth/providers/credentials';import {prisma} from './prisma';
export const authOptions:NextAuthOptions={session:{strategy:'jwt',maxAge:60*60*8},providers:[CredentialsProvider({name:'Email',credentials:{email:{},password:{}},async authorize(c){if(!c?.email||!c.password)return null;const user=await prisma.user.findUnique({where:{email:c.email},include:{roles:{include:{role:{include:{permissions:{include:{permission:true}}}}}}}});if(!user||user.status!=='ACTIVE')return null;const ok=await bcrypt.compare(c.password,user.passwordHash);if(!ok)return null;await prisma.auditLog.create({data:{userId:user.id,action:'LOGIN',entity:'User',entityId:user.id}});return{id:user.id,email:user.email,name:user.name,roles:user.roles.map(r=>r.role.name),permissions:[...new Set(user.roles.flatMap(r=>r.role.permissions.map(p=>p.permission.key)))]} as any;}})],callbacks:{jwt({token,user}){if(user){token.roles=(user as any).roles;token.permissions=(user as any).permissions}return token},session({session,token}){(session.user as any).id=token.sub;(session.user as any).roles=token.roles;(session.user as any).permissions=token.permissions;return session}}};
export function can(user:any,permission:string){return user?.permissions?.includes(permission)||user?.roles?.includes('Super Admin')}
